<!--components/area-header/area-header.wxml-->
<view class="header">
    <view class="title">{{title}}</view>
    <view class="more" bind:tap="onTapMore" wx:if="{{hasMore}}">
        <text class="moreText">更多</text>
        <van-icon name="arrow" class="moreIcon"/>
    </view>
</view>
